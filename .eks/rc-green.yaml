kind: ReplicationController
apiVersion: v1
metadata:
  name: green-app
  labels:
    app: green-app
spec:
  replicas: 2
  selector:
    app: green-app
  template:
    metadata:
      labels:
        app: green-app
    spec:
      containers:
        - name: cyan
          image: scgerkin/cyan-app:0.0.2-green
          ports:
            - name: spring-port
              containerPort: 8080
